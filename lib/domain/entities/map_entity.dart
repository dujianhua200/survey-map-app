// Domain entities for future business logic abstraction
// Currently data layer models are used directly for simplicity

class MapEntity {
  final String id;
  final String name;
  MapEntity({required this.id, required this.name});
}
